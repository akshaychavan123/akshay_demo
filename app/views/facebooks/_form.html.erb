<%= form_with(model: facebook) do |form| %>
  <% if facebook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(facebook.errors.count, "error") %> prohibited this facebook from being saved:</h2>

      <ul>
        <% facebook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
